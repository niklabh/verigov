.DS_Store

# Rust
target/

# Node
tools/node_modules/

# Demo runtime state (local artifact store and client installs)
data/
install/

# LaTeX build artifacts (the PDF is tracked)
paper/*.aux
paper/*.bbl
paper/*.blg
paper/*.log
paper/*.out
paper/*.fls
paper/*.fdb_latexmk
paper/*.synctex.gz
